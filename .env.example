# Variable NAMES only. Real values go in Dokploy secrets (server) or a local .env.
DEPLOY_MODE=server
PORT=8080
APP_DATA_DIR=/app/data
# Optional overrides (default under APP_DATA_DIR):
# IMAGES_DIR=/app/data/images
# GUESTS_DB=/app/data/guest_profile_images.db
# PACKS_DIR=/app/data/xml_packs
# XML_INDEX_DB=/app/data/xml_packs/xml_index.db
