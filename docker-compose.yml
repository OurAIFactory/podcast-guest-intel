services:
  web:
    build: .
    ports: ["8080:8080"]
    environment:
      - DEPLOY_MODE=local
      - PORT=8080
      - APP_DATA_DIR=/app/data
    volumes:
      - webdata:/app/data
    restart: unless-stopped
volumes:
  webdata:
